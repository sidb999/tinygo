#!/bin/bash
# tinygo flash -monitor -baudrate=9600 -target arduino-nano -scheduler tasks
# tinygo flash -monitor -baudrate=9600 -serial=usb -target=xiao -scheduler=tasks
# tinygo flash -monitor -baudrate=9600 -target=xiao -scheduler=tasks
# tinygo flash -monitor -baudrate=9600 -target=xiao -scheduler=tasks
